Card
	media buttons not clickable in editor when you add a new card
	button is not selectable either, so you can't set its type
	without drop shadow, there's no delineation... needs a default perimeter style
	horizontal cards render the image on the right instead of left.

Milestones
	spacing between milestones in editor shows gaps inconsistently
	default markers show on back end but not front end

Modal

Multibutton
	looks like hell in the editor.
	needs default background color. -- can't replicate
	set background doesn't work.
	open menus would do well with a high z-index.

Overlay
	can't click eyebrow, can arrow into it
	content spacing is wack
	background image doesn't cover -- can't replicate
	needs a focal point picker

Sidler
	second sidler looks odd when narrow

Tabs
	In the editor it isn't obvious which tab panel is linked to each tab.  outlines?
